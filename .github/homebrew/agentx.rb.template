# This file is rendered by the agentx release workflow's `homebrew` job
# from .github/homebrew/agentx.rb.template — DO NOT EDIT IN THE TAP.
cask "agentx" do
  version "__VERSION__"

  on_macos do
    on_arm do
      sha256 "__SHA256_DARWIN_ARM64__"
      url "https://github.com/agentserver/agentx/releases/download/v#{version}/agentx-aarch64-apple-darwin.tar.gz"
    end
    on_intel do
      sha256 "__SHA256_DARWIN_X86_64__"
      url "https://github.com/agentserver/agentx/releases/download/v#{version}/agentx-x86_64-apple-darwin.tar.gz"
    end
  end

  on_linux do
    on_arm do
      sha256 "__SHA256_LINUX_ARM64__"
      url "https://github.com/agentserver/agentx/releases/download/v#{version}/agentx-aarch64-unknown-linux-musl.tar.gz"
    end
    on_intel do
      sha256 "__SHA256_LINUX_X86_64__"
      url "https://github.com/agentserver/agentx/releases/download/v#{version}/agentx-x86_64-unknown-linux-musl.tar.gz"
    end
  end

  name "agentx"
  desc "Single-binary remote process/fs executor (hard-fork of codex exec-server)"
  homepage "https://github.com/agentserver/agentx"

  binary "agentx-#{Hardware::CPU.arch == :arm64 ? 'aarch64' : 'x86_64'}-#{OS.mac? ? 'apple-darwin' : 'unknown-linux-musl'}/agentx",
         target: "agentx"

  zap trash: "~/.agentx"
end
